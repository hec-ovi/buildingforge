# Box map

- root box: the exterior generator, see CONTRACT.md. Depends on atlas (parcel), connections (apertures), materials (material keys).
- planned inner boxes: `src/generator` (request -> layout -> mesh -> GLB + blueprint), `src/ui` (preview app: views/, widgets/, components/).
