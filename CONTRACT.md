# CONTRACT: exterior

Purpose: deterministically generates one building exterior as a GLB shell (empty inside, one separator plane per floor) plus a JSON blueprint of every exterior opening per floor.

Status: v0.35, implemented. Schemas stable to build against; additive fields may come, breaking changes go through the orchestrator.

## Conventions
- Units: meters. Ground plane XZ, +Y up, right-handed. 2D points `[x, z]`, CCW rings, first point not repeated (same as atlas).
- Outputs share the request footprint's coordinate frame; ground floor walking surface at Y = 0.
- Determinism: same request, same materials database and same texture options give a byte-identical GLB and blueprint JSON. No LLM, no wall clock, no ambient randomness.

## In
`generate(request: BuildingRequest, options?: GenerateOptions): { glb: Uint8Array, blueprint: Blueprint, textures: { mode, reason? } }`

Request: [schemas/building-request.schema.json](schemas/building-request.schema.json). Seed, parcel (footprint, street access point, max height), building (atlas type and tier verbatim, floor count, basements, optional per-floor kind labels), theme slug, required apertures (connections' aperture schema verbatim: face = parcel footprint segment index, absolute base, world-space cut polygon; kinds bridge, ac-tube, tunnel, wire-anchor), options (shape, balconies, fire escape, windows, signage marquee/logo, ad screens, roof artifacts, curtain profile).

`options.textures`: `{ mode?: "external" | "embed" | "keys", dir?, baseUrl?, source? }`. `dir` is the materials box root (defaults to `URBE_MATERIALS_DIR`, else the sibling `materials` box), `baseUrl` is the URI prefix written into the GLB before `themes/<theme>/assets/...`, and `source` is a preloaded materials source for callers with no filesystem (the browser preview) or `null` to force the keys fallback.

CLI: `npm run generate -- <request.json> <outDir> [--seed S] [--embed | --keys-only] [--materials DIR] [--materials-base URI]` writes `<buildingId>.glb` and `<buildingId>.blueprint.json`; external map URIs are written relative to the output directory by default.

Seeds: `generate` needs one, so a request with no seed (and no `--seed`) gets a random one rolled at the edge. The CLI prints the seed it used on every run and the preview keeps it in the seed field, so any building can be regenerated exactly.

Feasibility: [schemas/floor-constants.json](schemas/floor-constants.json) carries the per-type constants the generator enforces (type-to-family map, min and max floor height, min footprint area) plus the recipe for pre-computing a guaranteed-feasible floor count, with or without apertures.

Proportions: [schemas/proportions.json](schemas/proportions.json) is the published opening size table, also exported as `PROPORTIONS`. A floor's clear height is its floor-to-floor height minus `clearHeightAllowance`. Entrances stand by family: residential 2.4 to 2.9 m, venues (restaurant, coffee shop, commerce, mall) and hospitals 3.2 to 3.8 m, hotel and office 3.6 to 4.4 m, corpo 4.0 to 6.0 m (a double-height lobby door where the ground floor holds it), security 2.6 to 3.0 m, industrial 2.8 to 3.4 m, each capped by the ground floor's clear height; the ground storey keeps the room for its row's shortest door whenever the envelope leaves it. Every entrance is 2.6 to 3.2 m wide, a grand entrance (hotel, corpo and venues at rich and high_rich) 3.2 to 4.8 m. A standard-family window (residential, hotel, office, corpo, hospital, commerce) is 70 to 90 percent of the clear height by its family row on a 0.3 to 0.6 m sill; where sill plus glass would pass the clear height the sill drops to the row's floor first and the glass is trimmed last. A venue or lobby ground floor takes the `storefront` row instead: glass from a 0 to 0.3 m sill up to the clear height, one run per bay (per panel cell on a megablock), and balcony doors rise to the window head. `security` and `industrial` are the documented small-window exceptions, and the small deep scattered window is the poor tier's megablock alone, above its storefront. The generator enforces all of it (`E_INVARIANT`).

## Out
Blueprint: [schemas/blueprint.schema.json](schemas/blueprint.schema.json). Per floor (basements included): index, kind, elevation, height, CCW outline, openings (door | window | balconyDoor | aperture) positioned by outline edge + offset + sill, with curtain state, pane grid, spandrel band, door leaves, fitted door assembly and transom, balcony dimensions, material key. Plus signage, screens (each with the `edge` it is mounted on and its `standoff`), lights, facade style, exact face grids and artifacts, fire escape, roof artifacts, and the deduplicated material key list.

Floor `kind` slugs are atlas vocabulary verbatim: the parcel type itself (`restaurant`, `coffee_shop`, `commerce`, `mall`, `residential`, ...) on every typed floor, venue and ground floors included, so interior assigns real venue programs. The only slugs that are not atlas types: `lobby` and `entry` (non-venue ground floors), `basement`, and the special top floor of a tall rich hotel (`bar`) or corpo (`executive`). Request `floorKinds` pass through unchanged.

GLB shell:
- Binary glTF 2.0, one scene, named nodes: `floor:<index>/slab`, `wall:<floor>/<edge>`, `facade-relief`, `facade-artifacts`, `window:<opening-id>`, `door:<opening-id>` (with `door:<id>/frame` and one `door:<id>/leaf:<n>` per leaf), `balcony:<opening-id>` (same frame and leaf children), `aperture:<id>`, `anchor:<id>` (the wire mount, its origin the attach point), `roof`, `parapet`, `terrace:<floor>` (setback rings), `columns`, `roof-artifacts`, `facade-ac`, `bulkhead` (with `door:roof-bulkhead`, its `/frame` and `/leaf:0`), `base` (bottom cap), `signage:<n>`, `screen:<n>`, `light:<n>`, `fire-escape`.
- `options.glb: "merged"` swaps the node scheme for one mesh per material key (`merged:<theme/kind/tier>`), for runtime scale; door leaves, anchors and every `floor:<index>/slab` stay named nodes (the game swings the leaves and hangs wires on the anchors, the interior replaces the slabs) and the blueprint is identical either way. Default `"named"` is the canonical interchange.
- Empty inside except one separator plane per floor at its elevation, faced both ways (a one-sided slab is invisible through the glazing from below and the shell reads hollow). The `floor:<index>/slab` nodes are replaceable in both node schemes: interior re-emits them with stair and elevator holes under the same names.
- All triangles CCW front, outward normals; every mesh carries a NORMAL attribute (flat per face, matching the winding) so it shades correctly in any viewer. Every opening is a real hole in the wall band (grid-cut, watertight, no T-junctions), lined by a reveal back to the unit that fills it.
- A door is an assembly: `door:<id>/frame` holds the reveal lining and a mitred casing of three members in the canonical painted-steel `window-frame` material, and each swinging leaf is one node subtree, `door:<id>/leaf:<n>`, holding every mesh that moves with it, its glass pane included. The opening's `door` object publishes the selected set (`plain`, `layered`, `glazed-grid`, `industrial-ribbed`, `illuminated`), actual frame and recess dimensions, threshold and movement envelope. `doorRole` tells interior and navigation whether the opening is the main street entrance, a secondary public entrance or service access. The leaf node sits on its hinge (outer jamb, floor level), so rotating the node about its own Y swings the leaf. Loading shutters publish roller travel; other doors publish swing travel and the clear depth behind the threshold. Industrial ribs belong to the moving leaf and illuminated strips belong to the fixed frame. One coordinated set is selected per building role, then fitted to the accepted opening dimensions.
- Hotel, office, corpo and commerce frontages at least 18 m long repeat the complete public door set. The count comes from complete 8 m frontage cells, up to five doors; every copy keeps the main entrance's usable width, height, frame, material and movement clearance, starts on a metre seam, and retains a full pier from its neighbour. The access point still selects the main door. Remaining cells are secondary entrances, each published for an interior and navigation connection.
- A window unit is a frame profile with real reveal depth, a mullion grid splitting the opening into `panes.cols` x `panes.rows` panes, and the glass recessed behind the profile. Profile sections and pane limits are seeded once per building, so a facade is consistent and buildings differ.
- On a punched facade the glass fills the hole exactly and the frame ring straddles its edge: half the member over the glass, half over the wall, its back on the wall skin, so nothing shows between frame and wall or frame and glass. A curtain wall has no wall to sit on and its mullion is shared with the bay next door, so there the ring stays inside the bay and the glass is the field it leaves. Every frame member, jamb, mullion and door casing carries its map along its own length, so a jamb and the head it meets read as one rolled section.
- Facade style follows the family and the tier and is published as `facade.style`. `curtain-wall` (offices and corpo at rich and high_rich): one continuous glazed skin per face, thin mullions, an opaque spandrel band at every slab edge, the glass mostly transparent so the floor plates and interiors read through. `glass` (other rich, high_rich): clean mullioned glazing nearly flush with the panels. `panel` (mid): the unit sits back behind a reveal, thin ribs and floor bands, a few utility boxes. `megablock` (poor): heavy ribs on the panel grid with floor bands, many small windows scattered semi-irregularly inside the cells and deeply set, surface-mounted utility boxes.
- A curtain-wall face carries equal piers at both corners: the metre that does not divide the face is split between them, so two faces never meet on a wide band of bare wall. It is glazed corner to corner in bays that hang slab to slab: each bay opening carries `sill + height = the floor height`, a `spandrel` band at its bottom and a `head` band at its top, with the vision glass between them.
- Every floor line wears an opaque band, published as `facade.slabBand`: `below` metres under the walking surface and `above` metres over it. An interior slab laid on the elevation table lands inside it, so the slab never reads through the glazing between two window rows. A curtain wall splits its band across the line (the `head` of the bay below, the `spandrel` of the bay above, a door's transom stopping under the same band); a punched facade keeps `above` at 0, its sills already standing clear of the line, and its window heads stop `below` under the next one. Machine-checked on the blueprint and on the built glass (`E_INVARIANT`). Where a door takes the wall, the skin runs on over its head as that door's transom light: `transom` on the door opening is the glazed band above it (a 0.15 m head band between), so the run still belongs to one opening. An aperture keeps its solid collar. The spandrel panel and the mullions wear the `window-frame` material.
- `facade.panelModule` is the preferred panel pitch. Every floor face publishes its fitted grid in `facade.grids`: actual horizontal and vertical pitch, every seam including both endpoints, maximal opening-free `solid` intervals, and the centre and available width of every solid run at least 0.15 m wide. Box massing uses whole preferred modules. Where an aperture or parcel boundary fixes a non-divisible face or storey, the map and geometry use the fitted pitch at the nearest whole panel count. Ribs stand on those seams. Interior partitions meeting the facade use `partitionAnchors`, so they land on solid wall rather than through a pane, door or connection aperture. The grid is recomputed from the final opening set and checked before output. Everything hung on a facade is published as `facadeArtifacts` (kind, floor, edge, offset, sill, size, standoff): utility boxes on the `roof-artifact` material, and condenser units on `metal`.
- Condenser units: clusters of two to four housings on one bracket, one housing per metre module, hung on bare wall under a window or on the blank band between window rows, from the first floor up (the ground floor belongs to the street). Each is a housing with a grille face on a shelf carried by two struts; the grille wears `ac-unit` (an exact placement, one picture per face), the housing and the bracket `metal`. They are common on the poor and mid tiers and on homes and shops, rare on a corpo, and a face with no wall to hang a bracket on (a curtain wall) carries none. The cluster's box stays inside the parcel footprint the way a balcony does, and never covers an opening or a window; where the wall it wants carries relief (a rib, a floor band) the bracket stands proud of it and `standoff` says how far. Machine-checked before output.
- `facade.wallDepth` is how far the deepest opening unit on this building reaches behind the outline skin, in metres, measured on the built geometry: window reveals, frames, glass and curtain panels, door reveals and closed leaves (a closed leaf sits 0.12 to 0.175 m in), aperture collars. A consumer fitting the shell keeps everything it builds at least that far inside the outline. It reads 0.175 wherever a door leaf is the deepest unit and 0.3 to 0.5 on a megablock, whose windows sit deeper than the doors.
- The roof plane carries a stair-head cutout, `roof.bulkhead`: a rectangle centred on the top plate, square to the core's own axis, given as center, unit `axis`, width and depth. Its size follows the current stair riser comfort band, flight count, tread, landing and wall-thickness arithmetic in ../interior/schemas/core-feasibility.json, including one- and two-storey climbs. Interior lands its stair head there; roof artifacts and the walk space around the housing stay clear of it. Null when the plate is too small for access plus clearance.
- The housing over the cutout is a room: four walls carrying real thickness, faced on both sides so they read solid from the roof and from inside, a roof slab over them, and a real door on the face the stair arrives at (`door:roof-bulkhead` with its `/frame` and `/leaf:0`, hung and swung like every other door). Nothing else stands on the roof inside its walk space.
- A fire escape is the exception, not the rule: only residential and industrial buildings at the poor and mid tiers, two to seven floors, on a constant outline, and on `auto` (the default) only a seeded few of those wear one; `on` forces it where the building is eligible, `off` never. It hangs on the longest edge that is not the entrance facade, and its platform stack stands where every floor it passes steps out of a real opening onto it, windows before balcony doors; a run stops at the first floor it cannot serve and a face that serves no two floors in a row carries none. The blueprint publishes `edge`, `fromFloor`, `toFloor`, `offset` (u of the stack) and `width`.
- Signage is modular: one letter, one 1x1 cell, an N-letter text is N cells. `orientation: "horizontal"` is a framed marquee band over the entrance; `"vertical"` is a blade sign protruding edge-on from the facade, lettered on both faces, each face reading left to right from its own side. The orientation is decided by what fits the facade, then by building family and the facade's proportions, so a building always gets the same sign. A marquee, a logo plate and an ad screen are closed boxes standing off the wall: solid backs, so nothing reads through them from behind.
- Signs and ad screens land by scanning the face for a clear rectangle. A door, a balcony door, an aperture mouth and a wire anchor mount are never covered. Bare wall wins; when the face has none, the plate crosses relief (rib, column, floor band, spandrel) or, on a facade that is all glass, the glazing, and mounts proud of it: `standoff` on the blueprint entry is where its back plane sits. The scan tries the wanted size at the wanted place, then steps along the face and up and down, then shrinks, then relocates to the next face in the entrance ranking; a sign with nowhere to go is dropped rather than forced. Machine-checked before output. The frame plate wears the `signage` material; each glyph cell is a square quad on the `letter-atlas` material, UV-cut to its character out of ../materials' 8 x 6 row-major sheet (`index = charset.indexOf(char.toUpperCase())`, rect `[(index % 8) / 8, floor(index / 8) / 6, 1/8, 1/6]`). The charset is `ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789-.,'!?:/&+` plus a trailing space; a character outside it reads as that space, which reserves its cell width and gets no quad. Sign text is a request parameter.
- Every material is named by the canonical key `theme/kind/tier` (lowercase slugs). Kinds used: wall, wall-trim, column, window-glass, window-frame, curtain, door, door-glass, balcony-slab, balcony-rail, roof, floor-slab, parapet, signage, letter-atlas, ad-screen, light-fixture, fire-escape, aperture-frame, roof-artifact, ac-unit and metal (a condenser unit's grille face and the painted housing and bracket under it).
- Curtain-wall bays are the one opening whose height is the floor height exactly, not a 0.05 m quantum: quantizing there would leave a wall sliver under every slab.
- Tiled materials get world-scale UVs (1 UV unit = 1 tile meter, planar per face, origin at the face's bottom-left, U along the face's own horizontal edge) so textures never stretch. Every horizontal surface of one building (roof, floor slabs, terraces, the housing's own slab) shares one grid: origin at the ground plate's corner, U along the core's axis, so a roof joint runs parallel to the parapet instead of cutting across it, and slabs seen through the glazing line up with the roof. opening and style dimensions are quantized to 0.05 m, positions to millimeters. Exact-placement materials (ad-screen, signage, letter-atlas, window glass) get exact 0..1 UVs over their quad (a sub-rect of it for a glyph cell), never a partial tile. V follows the glTF convention: v = 0 is the top row of the map, so an atlas cell's top edge is its smaller v.

Textures. The default export is a finished exterior: every key resolves through ../materials into real maps (basecolor, normal, occlusion, emission where the entry has one, plus its metallic and roughness factors, transmission and IOR for glass). Tiled entries carry a `KHR_texture_transform` scale of 1 / tiling worldSize over the world-meter UVs; exact entries get clamped 0..1 UVs and no transform. The map variant is picked deterministically per material key from the seed, except the kinds that must read the same everywhere: `window-frame`, `door`, `roof`, `floor-slab` and `light-fixture` always take the canonical variant, so a frame section is flat painted steel on every building, a deck is a solid surface with no joint to cut, and an exterior lantern is the lamp its housing is shaped for. Emissive strength is the entry's own; nothing is damped here. `textures.mode` on the result says which mode the GLB carries:
- `external` (default): map URIs written against `baseUrl`, nothing embedded.
- `embed`: the maps packed into one self-contained GLB.
- `keys`: material names only, for a consumer that resolves them itself (the engine runtime).
With no materials database at the configured path, output falls back to `keys` and `textures.reason` says so, so the box still runs standalone.

## Errors
Thrown as `ExteriorError { code, message, details? }`:
- `E_SCHEMA`: request fails schema validation; message names the path.
- `E_FOOTPRINT_INVALID`: footprint self-intersects, has under 3 points, or zero area.
- `E_FOOTPRINT_TOO_SMALL`: usable area below the type's minimum.
- `E_ENVELOPE_TOO_LOW`: floors x the type's minimum floor height exceeds maxHeight; the message carries the exact numbers.
- `E_FLOORKINDS_MISMATCH`: floorKinds length differs from floors.
- `E_APERTURE_UNREACHABLE`: face index outside the footprint, base outside the vertical range, an aperture taller than the type's max floor height, or no legal floor split exists for the requested floor count; the message names the feasible count range.
- `E_APERTURE_INVALID`: cut polygon off its face plane or inconsistent with u/width/height.
- `E_APERTURE_OVERLAP`: two aperture cuts overlap on the same face.
- `E_SIGNAGE_TEXT_TOO_LONG`: the text needs more letter cells than the facade holds either way round; the message names both limits.
- `E_CORE_PLATE`: the floor plates on this lot share no position for the core the interior needs for this floor count and its exact storey heights; the message names the band reached and the nearest core size, so the parcel can be retyped or resized.
- `E_MATERIAL_UNRESOLVED`: the materials theme has no entry for a key the building uses, or embedded textures were asked for without the database on disk.
- `E_INVARIANT`: post-generation coherence check failed; exterior bug, report with the request.

## Invariants
- Roof housing: sized from ../interior/schemas/core-feasibility.json (stair column width or shaft depth by the interior's recipe, square, plus a metre), placed at the top plate's centroid on the selected core frame; the interior centres its stair under it.
- Module grid: `MODULE` (0.5 m, src/rules/tables.ts) sizes every storey (where the envelope allows), bay, window width and height, sill, balcony door and corner margin, and the head band (clearHeightAllowance) is one module; wall UVs start at each facade's corner and storey line, so a panel pattern whose panel is a module frames every opening without a cut tile. Proportion invariants are met to the nearest half module.

- Face i is the vertical quad over parcel footprint segment i -> i+1 (connections convention). Every face carrying an aperture keeps its wall exactly on that segment; shape variation (octagon, cylinder, pyramid, inset) applies to aperture-free buildings, setbacks only above the topmost aperture.
- Every floor plate shares one core placement. A plate is the floor outline inset by the facade depth the interior publishes for the style. Core length is computed from the actual one- and adjacent-two-storey climbs, using the published riser comfort band, whole flight count, tread, landing, wall thickness, riser, service and egress arithmetic. The preflight tries the standard lift row, compact lift columns, then a stair-only walkup within its floor cap. It starts on the longest ground-outline edge and, only when that frame cannot fit, sweeps the same 5 degree fallback frames as interior v0.25. The core band is intersected across every floor, so setbacks cannot pass independently at different positions. Massing aims half a metre beyond the bare fit for snapping and steps inward only while the shared placement remains. A lot with no placement is `E_CORE_PLATE`, named before anything is built.
- Every bridge, ac-tube and tunnel aperture yields exactly one `aperture` opening carving exactly the given cut, with a floor walking surface at exactly its base; that floor is tall enough to contain the aperture's full vertical extent. A wire anchor cuts no hole: the GLB carries node `anchor:<id>` with its origin at the attach point and the mount under it, a plate sized to the cut (0.3 to 0.6 m square, 3 cm thick) plus the lug the wire hangs from, in the `window-frame` material, standing proud of whatever the face carries there. A punched facade keeps the plate's footprint clear of openings; a curtain wall glazes straight across it, so the mullion grid runs whole. The per-floor elevation table in the blueprint is final; interior consumes it.
- Floor elevations are contiguous: `elevation[i+1] = elevation[i] + height[i]`; ground floor at 0.
- One opening owns one stretch of an edge: two openings on the same floor and edge never share any of it, whatever their heights, so a consumer reads each facade as a run of exclusive intervals. Min 0.3 m pier between openings. Every opening lies entirely inside its edge with non-negative offset and inside its floor's height (machine-checked before output).
- Every door publishes exactly one navigation role. Only doors publish one. Repeated public entrances share their main entrance's accepted dimensions and fitted set.
- Every `facade.grids` entry closes exactly on its face length and storey height. Its solid intervals never cross an opening, and every partition anchor is contained by the solid run whose width it publishes.
- Every opening the proportion table covers is the size that table promises: entrance height in its family range and at least the standard width its edge allows, window height at or above its share of the clear height on a sill inside the family range, storefront glass reaching the clear height on a sill inside its row (machine-checked before output).
- The entrance goes to a street-facing edge at least 3 m long when one exists (true point-to-segment distance from the access point, never a corner-touching sliver), and its zone is reserved before any window fill.
- Every balconyDoor carries balcony dimensions; balconies protrude beyond the outline but stay inside the parcel footprint. Each candidate slab is checked before its door replaces a window. An aperture-pinned or otherwise constrained face with no room keeps the window instead.
- The structure never exceeds the parcel footprint; it need not fill it. Whole candidate edges are checked against every interval of a concave boundary, including massing rings, roof housings, roof artifacts and condenser runs, so an edge cannot leave and re-enter through a notch between otherwise valid corners.
- Blueprint floors match GLB geometry exactly (same outlines, same opening rectangles).

## Preview
`npm run preview`: vite app, loads a fixture, generates in-browser and shows the finished textured building at first load framed from outside (the dev server serves the materials box at `/materials`). Two cameras: orbit, and a street eye standing 1.7 m above the pavement in front of the entrance, which is where door and window proportions are judged. Per-floor clipping, a flat kind-coloured look and wireframe for reading geometry, opening highlighting. UI in `src/ui/` with `views/`, `widgets/`, `components/`.

## Depends on
- ../atlas/CONTRACT.md (parcel: footprint, access point, envelope, type, tier)
- ../connections/CONTRACT.md (aperture constraints)
- ../materials/CONTRACT.md (material key resolution)
